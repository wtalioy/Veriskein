//! Placeholder crate for later local IPC protocol definitions.
//! The crate exists early so daemon internals do not accidentally become the
//! de facto external protocol before IPC shapes are designed.
